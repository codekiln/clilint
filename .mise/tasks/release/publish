#!/usr/bin/env bash
#MISE description="Tag the version prepared by a merged release pull request"
set -euo pipefail

config=$(mise run release:config)
release-plz release --config "$config" --output json
