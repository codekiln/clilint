# fixtures/

Example CLIs used to develop and test the linter — both conformant and deliberately nonconformant
tools. Fixtures pin the expected findings and scores for known inputs, so changes to
[`rules/`](../rules/) or [`probes/`](../probes/) can be validated against stable examples.
