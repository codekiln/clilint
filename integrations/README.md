# integrations/

Ways to run CLI Lint where CLIs are built and shipped — a GitHub Action, a pre-commit hook, and
agent skills that wire `clilint` into coding assistants. These make conformance a checkable gate
rather than a one-off manual run.
