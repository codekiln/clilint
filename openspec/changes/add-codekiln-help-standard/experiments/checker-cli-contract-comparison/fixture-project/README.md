# Example CLI

This fixture demonstrates a small command-line tool.

Run `target_cli.py serve` to start it.

The tool supports configuration, output selection, several server modes,
plugins, themes, remote synchronization, report generation, and many other
features. New users may need help deciding which features matter for their
first task.
