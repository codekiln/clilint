{
  "generated-cli": {
    "description": "Default profile for a CLI newly written or modified (often by an agent). Enforces every rule, including the agent-oriented ones, so the result works well for humans and for unattended callers.",
    "rules": "all"
  },
  "modern-cli": {
    "description": "A general profile for a human-facing modern CLI. Enforces the core standard but treats the agent-specific rules as optional.",
    "rules": "all",
    "exclude": ["CLI-AGENT-001", "CLI-AGENT-002"]
  }
}
