# spec/

The normative **CLI Lint** conformance standard, in prose. Defines each rule's intent, the
`CLI-` identifier scheme, severities, scoring, and the conformance profiles. The machine-readable
form of these rules lives in [`rules/`](../rules/); the prose here is the source of truth for what
each rule means.
