"""clilint — a CLI conformance linter and the reference implementation of the CLI Lint standard."""

__version__ = "0.0.1"

# The standard version this build checks against. Rules are versioned with the standard.
STANDARD = "CLI Lint 0.0.1"
